name: first workflow
on: [workflow_dispatch, push]
jobs:
  first-job:
    runs-on: ubuntu-latest
    name: The first job
    steps:
      - uses: sofusalbertsen/custom-action@main
        with:
          myInput: "sofus"
